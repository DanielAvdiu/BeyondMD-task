server {
    listen 80;  # Updated to listen on port 80

    location / {
        root /var/www/frontend;
        index index.html index.htm;  # Added index files
        try_files $uri $uri/ /index.html;  # Added to handle client-side routing
    }
}
